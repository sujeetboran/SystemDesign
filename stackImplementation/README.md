Vec_stack
    normal stack class which we have but via using vector
    to build the code you can use the command :- g++ -std=c++17 vec_stack.cpp -o vec_stack
List_stack
    normal stack class which we have but via using list